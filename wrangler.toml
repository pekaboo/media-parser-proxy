name = "pekaboo-namexxx"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# 使用 Workers 原生 fetch，无需 nodejs_compat
compatibility_flags = []

# 开发: npx wrangler dev
# 部署: npx wrangler deploy

[observability]
enabled = true

# 可选：环境变量（也可在 dashboard 配置）
[vars]
DEFAULT_TIMEOUT_MS = "30000"
